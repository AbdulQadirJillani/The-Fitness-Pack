---
import about from '../assets/about.png'
import { FaDumbbell } from "react-icons/fa";
import { IoPerson } from "react-icons/io5";
import { FaConciergeBell } from "react-icons/fa";
import { Image } from 'astro:assets';
---

<div class='aboutcontainer'>
  <picture>
    <Image src={about} alt="" />
  </picture>
  <div>
    <span>About our Gym</span>
    <h1>FIGHT FOR FITNESS</h1>
    <p>The extension comes with three pre-built pages to help you get started faster. You can change the text and images and you're good to go.</p>
    <ul>
      <li><FaDumbbell /><span>A studio that offers you more</span></li>
      <li><IoPerson /><span>3 professional trainers</span></li>
      <li><FaConciergeBell /><span>Fitness for everyday people</span></li>
    </ul>
  </div>
</div>

<style>
  .aboutcontainer {
  overflow-x: clip;
  min-height: 100svh;
  display: flex;
  justify-content: space-evenly;
  align-items: center;
  gap: 1em;
  background-color: black;
  color: white;
}

.aboutcontainer img {
  height: 75vh;
  width: 100%;
  translate: -50%;
  opacity: 0.3;
  border-radius: 10px;
  object-fit: contain;
  animation: imgmotion linear;
  animation-timeline: view();
}

.aboutcontainer div {
  width: 42ch;
  translate: 50%;
  opacity: 0.3;
  animation: divmotion linear;
  animation-timeline: view();
}

.aboutcontainer div > span {
  font-size: 0.8em;
  color: var(--textclr);
}

.aboutcontainer div h1 {
  font-size: 1.8em;
  font-weight: bolder;
}

.aboutcontainer p {
  margin-bottom: 1em;
  padding: 0.5em 0;
}

.aboutcontainer div ul li {
  display: flex;
  gap: 0.8em;
  font-size: 1.2em;
  padding: 0.6em 0;
}

.aboutcontainer div ul li > :first-child {
  color: var(--textclr);
  font-size: 1.1em;
}

@keyframes imgmotion {
  50%, 100% { translate: 0%; opacity: 1; }
}

@keyframes divmotion {
  50%, 100% { translate: 0%; opacity: 1; }
}

@media (max-width:768px) {
  .aboutcontainer {
    flex-direction: column;
    gap: 1em;
    padding: 1em;
  }

  .aboutcontainer div {
    width: auto;
  }
}
</style>